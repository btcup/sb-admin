#include <iostream>
#include <cmath>
using namespace std ;
int main ()
{
    int  x,a,b,c,d,total,average,largest;
        if (x=0 , x<=5 , x++);
            cout << "Give the number of passengers of day " << x <<" : " << endl;    
            cin >> a,b,c,d ;
        
        else (x>=5);   
             cout << "  " <<endl;
   
        cout << "Total passenger for route 101 is : " << total << endl;    
    
        cout << "The average number of passenger for all day and all routes is : " << average << endl;
             average = ()/20; 
        cout << "The largest nember of passenger for Thu is : " << largest << endl;
        
        system("pause");
        return 0;
}
