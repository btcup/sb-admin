#include<iostream>
#include<string>
using namespace std;

string Compare(strig Word,string strCond)
 { 
  if(Word = strCond)
  word = true;
  else
  word = false;
 }
return Compare;

string CountWord(string strln,string strCond)
{
       int s;
       s = strln.size();
return s;
}
string strCond();  
{    
   for(int i=0;i=text.size();i++)
   {
     cout<<"Count condition: ";
     string text2;
     getline(cin,text2,'\n');
     
     if(text2[i]+'*'||'*')
     strCond = text2.size[i];
     cout<<"strCond";
     if else('*'+text[i]||'*')
     
     else
     cout<<"Unknown condition bye...";break;     
   }

int main()
 {
   string strln;
   cout<<"Text :     ";
   getline(cin,strln,'\n');
   
   Word(strln,text2);
   strCond();
   CountWord(strln,strCond);
   int s;
   cout<<s;
   Compare(Word,strCond);
  
   system("pause");
   return 0;   
 }
      
     
      
      
      
      
      
      
