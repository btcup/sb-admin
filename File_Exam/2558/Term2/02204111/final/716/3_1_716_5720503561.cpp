#include<iostream>
using namespace std;
void count(string a)
{
for(
cout<<"It is "<<
}
int main()
{
    do
    string a;
    cout<<"Can I help you? : ";
    getline(cin,a)
    if(a.find("count",0)
    count(a);
system("pause");
return 0;
}
