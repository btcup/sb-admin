#include<iostream>
using namespace std;
int main();
 {
    int[N][M];
    int x,i,j,N,M;
        cout<<"Enter M and N:";
        cin>>i>>j;
        for(i=2;i<x;i++)
        {
                        for(j=2;j<x-i-1;j++)
                        cout<<"";
                        for(j=2;j<x+i*2;j++)
                        {
                                            if(j>i&&j<(x+i-1)*2&&i!=2&&i!=x-1&&i<=15)
                                            cout<<[N][M];
                                            else
                                            cout<<"Error.please input M and N between2-15.";
                                            }
                                            cout<<endl;
                                            }
                                            system("pause");
                                            return 0;
                                            }
                                            
