#include<iostream>
using namespace std;
int main()
{
    int age,price;
    cout<<"Normal price:";
    cin>>age;
    price=0;
    if(age>2)
    {
              if(age>2)
              {"price=price*100%";}
              else
              {"price=price+(age-0)*100%";}
              }
              if(age>2)
{
              if(age>12)
              {"price=price+70%";}
              else
              {"price=price+(age-2)*70%";}
              }
              if(age>12)
{
              {"price=price+(age-12)*50%";}
              }
              if(age>2)
              {"price=(price*100%)";}
              else
              {"price=price+(age-0)*100%";}
              {
              if(age>2)
{
              if(age>10)
              {"price=price+(50%)";}
              else
              {"price=price+(age-2)*50%";}
              }
              if(age>10)
{
              {"price=price+(age-10)*0%";}         
}             
cout<<"How old are you:"<<"you have to pay:"<<endl;
system("pause");
return 0;
}

