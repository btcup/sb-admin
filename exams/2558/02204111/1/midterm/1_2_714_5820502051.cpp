#include <iostream>
using namespace std;
int main() 
{
    int a[0],s,i,d;
    cout<<"Enter employee ID and salary : ";
    cin>>a[0]>>s;
    for(i=0;;i++){
    if(a==0||s==0)
    { cout<<"number of employee in the company = "<<a[-1]<<endl;
      cout<<"number of salaries between 15,000 - 25,000 bath = "<<d<<endl;
      brake;}
    else{
    
    cout<<"Enter employee ID and salary : ";
    cin>>a[0]>>s;
    if(s>=15000)
    {d++
    }
    }
}

system("pause");
return 0;
}
