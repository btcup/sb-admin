#include <iostream>
using namespace std;
int main()
{
    char E='e',A='a',C='c', L='l',M='m',S='s',T='t',G='g',V='v';
    char n,m,z,x,y;
    int sum;
    
    cout<<"Welocom to Cup-with-love Coffee!!"<<endl;
    do
    {
    cout<<"Enter your menu (E/A/L/C/M):"<<n<<endl;
    cin>>n;
    sum=0;
    if(n==E)
         {
       
          S=40;
          T=50;
          G=60;
          V=70;
         cout<<"Enter size (S/T/G/V):"<<m<<endl;
          cin>>m;
           cout<<"How many cups?:"<<z<<endl;
           cin>>z;
           
         if (m==S)
         {
         sum=(sum+S)*z;
         cout<<sum<<endl;
         }
         else if (m==T)
         {
         sum=(sum+T)*z;
         cout<<sum<<endl;
         }
         else if (m==G)
         {
         sum=(sum+G)*z;
         cout<<sum<<endl;
         }
         else if (m==V)
         {
         sum=(sum+V)*z;
         cout<<sum<<endl;
         }
         
         }
       else if(n==A)
         {
       
          S=40;
          T=50;
          G=60;
          V=70;
         cout<<"Enter size (S/T/G/V):"<<m<<endl;
          cin>>m;
           cout<<"How many cups?:"<<z<<endl;
           cin>>z;
           
         if (m==S)
         {
         sum=(sum+S)*z;
         cout<<sum<<endl;
         }
         else if (m==T)
         {
         sum=(sum+T)*z;
         cout<<sum<<endl;
         }
         else if (m==G)
         {
         sum=(sum+G)*z;
         cout<<sum<<endl;
         }
         else if (m==V)
         {
         sum=(sum+V)*z;
         cout<<sum<<endl;
         }
         }
          else if(n==A)
         {
       
          S=40;
          T=50;
          G=60;
          V=70;
         cout<<"Enter size (S/T/G/V):"<<m<<endl;
          cin>>m;
           cout<<"How many cups?:"<<z<<endl;
           cin>>z;
           
         if (m==S)
         {
         sum=(sum+S)*z;
         cout<<sum<<endl;
         }
         else if (m==T)
         {
         sum=(sum+T)*z;
         cout<<sum<<endl;
         }
         else if (m==G)
         {
         sum=(sum+G)*z;
         cout<<sum<<endl;
         }
         else if (m==V)
         {
         sum=(sum+V)*z;
         cout<<sum<<endl;
         }
         }
          else if(n==L)
         {
       
          S=40;
          T=50;
          G=60;
          V=70;
         cout<<"Enter size (S/T/G/V):"<<m<<endl;
          cin>>m;
           cout<<"How many cups?:"<<z<<endl;
           cin>>z;
           
         if (m==S)
         {
         sum=(sum+S)*z;
         cout<<sum<<endl;
         }
         else if (m==T)
         {
         sum=(sum+T)*z;
         cout<<sum<<endl;
         }
         else if (m==G)
         {
         sum=(sum+G)*z;
         cout<<sum<<endl;
         }
         else if (m==V)
         {
         sum=(sum+V)*z;
         cout<<sum<<endl;
         }
         }
          else if(n==M)
         {
       
          S=40;
          T=50;
          G=60;
          V=70;
         cout<<"Enter size (S/T/G/V):"<<m<<endl;
          cin>>m;
           cout<<"How many cups?:"<<z<<endl;
           cin>>z;
           
         if (m==S)
         {
         sum=(sum+S)*z;
         cout<<sum<<endl;
         }
         else if (m==T)
         {
         sum=(sum+T)*z;
         cout<<sum<<endl;
         }
         else if (m==G)
         {
         sum=(sum+G)*z;
         cout<<sum<<endl;
         }
         else if (m==V)
         {
         sum=(sum+V)*z;
         cout<<sum<<endl;
         }
         }
         
         cout<<"Do you want other menu?( Y/N)"<<x<<endl;
         cin>>x;
               while(x=='N')
                   {
           
                      cout<<"Aru you a mamber? (Y/N)"<<y<<endl;
                      cin>>y;
                             while(y=='Y')
                             {
                                  cout<<"Thank you"<<endl;
                               }
                             cin>>x;
                             }
                             
        }
         while('Y')
         cout<<"To
         system ("pause");
    return 0;
}
