#include <iostream>
 using namespace std;
 int main()
{
     int i,x,y;
     
     for (x=1;x!=0;i++)
     {
     cout << "Enter employer ID and Salary : ";
     cin >> x >> y;
         
         i++;
         
     }        
     
     {        if (x != 0)
              {cout << "Number of employers in the company = " << ? <<endl;}
              
              if (y >= 15000 && y <= 25000 )
              {cout << "Number of salaries between 15,000 - 25,000 bath = " << ? << endl;}  
              
              
     }
     system ("pause");
     return 0;
}
