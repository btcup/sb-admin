#include <iostream>
using namespace std;
int main()
{
    int x,y,a,b;
    cout<<"Enter decimal number : ";
    cin>>x;
    while (x>0)
    {
     if (x>0)
     cout<<"Enter base (2-9): ";
     cin>>y;
     cout<<x<<"b10 is ";
 
     }
system ("pause");
return 0;
}
