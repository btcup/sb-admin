#include<iostream>
using namespace std;

int main(){
int d,b;
do{
    cout<<"Enter decimal number: ";
    cin>>d;
    cout<<"Enter base (2-9): ";
    cin>>b;
    }while(d>=0&&b>=0);
    system("pause");
    return 0;
}
