#include<iostream>
using namespace std;
int main()
{
    int x,y,z;
    cout<<"enter an integer : ";
    cin>>x;
    z=x*x;
    cout<<"number of digit is "<<y<<endl;
    if (z==x)
    {
    cout<<x<<" is a target number."<<endl;
}
    else if (z!=x)
    {
         cout<<x<<" is a not target number."<<endl;
         }
system("pause");
return 0;
}
