//5920550074 Phalakorn Manonom
#include <iostream>
#include <cmath>
using namespace std;
int main ()
{
  int N;
  if
         cout<<"Enter an integer :"<<endl;
          cin>>153 == pow(1,2)+pow(5,2)+pow(3,2);
          cin>> 1634 == pow(1,2)+pow(5,2)+pow(3,2);
          cin>> 372 !== pow(1,2)+pow(5,2)+pow(3,2);
          cout<<"Enter an integer :"<<endl ; 
          cout<<"Nume of digit is 3 "<<endl;
          "371 is a target number."<<endl;
        }
        {
        else
           cout<<"Enter an integer :"<<endl  ;
           cin>> 153 == pow(1,2)+pow(5,2)+pow(3,2);
           cin>> 1634 == pow(1,2)+pow(5,2)+pow(3,2);
           cin>> 372 !== pow(1,2)+pow(5,2)+pow(3,2); 
           cout<<"Nume of digit is 3 "<<endl;
           "372 is a target number."<<endl;
        }
        {
        else
           cout<<"Enter an integer :" <<endl ;
           cin>> 153 == pow(1,2)+pow(5,2)+pow(3,2);
           cin>> 1634 == pow(1,2)+pow(5,2)+pow(3,2);
           cin>> 372 !== pow(1,2)+pow(5,2)+pow(3,2); 
           cout<<"Nume of digit is 4 "<<endl;
           "1634 is a target number."<<endl;
        }
        {
        else
          cout<<"Enter an integer :"<<endl  ;
          cin>> 153 == pow(1,2)+pow(5,2)+pow(3,2);
          cin>> 1634 == pow(1,2)+pow(5,2)+pow(3,2);
          cin>> 372 !== pow(1,2)+pow(5,2)+pow(3,2); 
          cout<<"Nume of digit is 5 "<<endl;
          "54321 is a target number."<<endl;
        }
system ("pause");
return 0;
}
