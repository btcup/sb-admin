#include<iostream>
using namespace std;
int main()
{
     char car;
     double y,d;
     cout<<"Enter car model : ";
     cin>>car;
     if(car='A')
       cout<<"Enter namber of years(1-6): "
       cin>>y;
          if(y>=1&&y<4)
             {
                y=
             }
          if else(y>=4&&y<=6)
             {
                y=       
             }
           else
           {
               cout<<"Error!, namber of years : "
           }     
     else     
       cout<<("Error!Car model");
      if(car='B')
       cout<<"Enter namber of years(1-6): "
       cin>>y;
          if(y>=1&&y<4)
             {
                y=
             }
          if else(y>=4&&y<=6)
             {
                y=       
             }
          else
           {
               cout<<"Error!, namber of years : "
           } 
     else     
       cout<<("Error!Car model");
       if(car='C')
       cout<<"Enter namber of years(1-6): "
       cin>>y;
          if(y>=1&&y<4)
             {
                y=
             }
          if else(y>=4&&y<=6)
             {
                y=       
             }
     else     
       cout<<("Error!Car model");
      if(car='J')
       cout<<"Enter namber of years(1-6): "
       cin>>y;
          if(y>=1&&y<4)
             {
                y=
             }
          if else(y>=4&&y<=6)
             {
                y=       
             }
          else
           {
               cout<<"Error!, namber of years : "
           } 
     else     
       cout<<("Error!Car model");
     
     system("pause");
     return 0;     
}
