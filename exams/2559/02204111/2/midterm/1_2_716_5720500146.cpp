#include<iostream>  //5720500146 Thanakorn Norasing
#include<cmath>
using namespace std;
int main()
{
    int x,y=0,z;
        do
        {
                cout<<"Enter N: ";
                cin>>x;
        }
        while(x<=0);
        {
                    x%y==0;
                    y++;
                    
                    if(x=y)
                    cout<<"The greatest factor of "<<x<<" is "<<y<<"---> prime"<<endl;
                    else
                    cout<<"The greatest factor of "<<x<<" is "<<y<<endl;
        }                      
    
    
    system("pause");
    return 0;
}
