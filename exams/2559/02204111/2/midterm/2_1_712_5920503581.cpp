//"5920503581suchada khampoomuang"
#include<iostream>
using namespace std;
int main()
{
    char A,B,C;
    int x=25/100,model,salary,year,financing amount,amount of interest,;
    cout<<"enter car model:";
    cin>>model;
    cout<<modle;
    if((financing amount+amount of interest)/year>0.5*salary)
{
    else if(model==A)
    {
     cout<<"enter number of year(1-6):";
     cin>>year;
     cout<<"enter your salary:";
     cin>>salary;
     cout<<"financing amount:"<<1385000-(1385000*x);
     cout<<"amount of interest:"<<((1385000-(1385000*x))*2.09)*year;
     cout<<"monthly payment:"<<((1385000-(1385000*x))+(((1385000-(1385000*x))*2.09)*year))/year
     if((((1385000-(1385000*x))+(((1385000-(1385000*x))*2.09)*year))/year)>0.5*salary)
       {
     cout<<"good decision,enjoy your new car";
       }
     else
       {
     cout<<"the monthlypayment is over than 50% of your salary!!";
       }
     }
      else if(model==B)
    {
     cout<<"enter number of year(1-6):";
     cin>>year;
     cout<<"enter your salary:";
     cin>>salary;
     cout<<"financing amount:"<<511500-(511500*x);
     cout<<"amount of interest:"<<((511500-(511500*x))*2.09)*year;
     cout<<"monthly payment:"<<((511500-(511500*x))+(((511500-(511500*x))*2.09)*year))/year
     if((((511500-(511500*x))+(((511500-(511500*x))*2.09)*year))/year)>0.5*salary)
       {
     cout<<"good decision,enjoy your new car";
       }
     else
       {
     cout<<"the monthlypayment is over than 50% of your salary!!";
       }
        else if(model==C)
    {
     cout<<"enter number of year(1-6):";
     cin>>year;
     cout<<"enter your salary:";
     cin>>salary;
     cout<<"financing amount:"<<738000-(738000*x);
     cout<<"amount of interest:"<<((738000-(738000*x))*2.09)*year;
     cout<<"monthly payment:"<<((738000-(738000*x))+(((738000-(738000*x))*2.09)*year))/year
     if((((738000-(738000*x))+(((738000-(738000*x))*2.09)*year))/year)>0.5*salary)
       {
     cout<<"good decision,enjoy your new car";
       }
     else
       {
     cout<<"the monthlypayment is over than 50% of your salary!!";
       }
}
    else
    cout<<"the monthlypayment is over than 50% of your salary!!";
    system("pause");
    return 0;
}
