//Miss Kamonchanok Adisorn 5620550117
#include <iostream>
using namespace std ;
int main ()
{
    int l,c,R,b,W;
    float A;
    if (A==6.5)
    {
               if (c>l, c-l==W,(c-l)*A==b)
               cout<<"Enter last unit :";
               cin>>l;
               cout<<"Enter current unit :";
               cin>>c;
               cout<<"Unit used = "<<W;
               cout<<"Electricity charge (bath) = "<<b;
               else if (l>c)
               cout<<"Enter last unit :";
               cin>>l;
               cout<<"Enter current unit :";
               cin>>c;
               cout<<"Invalid last unit!";
               else if (l<0)

               
               }
    else if (A==7)
    {
               if (c>l)
               c-l==U
               (c-l)*A==b
               cout<<"Enter last unit :";
               cin>>l;
               cout<<"Enter current unit :";
               cin>>c;
               cout<<"Unit used = "<<W;
               cout<<"Electricity charge (bath) = "<<b;
               else if (l<0)
               cout<<"Invalid last unit!";
               else if (c<0)
               cout<<"Invalid last unit!";
               else if (c<l)
               cout<<"Enter last unit :";
               cin>>l;
               cout<<"Enter current unit :";
               cin>>c;
               cout<<"Invalid last unit!";
    
    
    system ("pause");
    return 0;
}
