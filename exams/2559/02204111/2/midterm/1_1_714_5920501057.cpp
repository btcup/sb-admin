//5920501057 natthanit jirapreda
#include <iostream>
using namespace std;
int main()
{
    char model,A,B,C,J;
    double a,b,Financing,Amount,Monthly,down,year;
   
    if(model=='A')
    {
                     cout<<"-------Car lease calculator -------")<<endl;
                     cout<<"Enter car model :";
                     cin>>model;
                     cout<<"Enter number of years (1-6) :";
                     cin>>a;
                     if(year>6)
                     cout<<"Error!,number of years is not in range");
                     cout<<"Enter percentage of down payment :";
                     cin>>b;
                     cout<<"------------------------------------");
                     
    
    }
    if(model=='B')
    {
                     cout<<"-------Car lease calculator -------")<<endl;
                     cout<<"Enter car model :";
                     cin>>model;
                     cout<<"Enter number of years (1-6) :";
                     cin>>a;
                     if(year>6)
                     cout<<"Error!,number of years is not in range");
                     cout<<"Enter percentage of down payment :";
                     cin>>b;
                     cout<<"------------------------------------");
    }
    if(model=='C')
    {
                     cout<<"-------Car lease calculator -------")<<endl;
                     cout<<"Enter car model :";
                     cin>>model;
                     cout<<"Enter number of years (1-6) :";
                     cin>>a;
                     if(year>6)
                     cout<<"Error!,number of years is not in range");
                     cout<<"Enter percentage of down payment :";
                     cin>>b;
                     cout<<"------------------------------------");
    }
    if(model=='(J')
    {
                     cout<<"-------Car lease calculator -------")<<endl;
                     cout<<"Enter car model :";
                     cin>>model;
                     cout<<"Enter number of years (1-6) :";
                     cin>>a;
                     if(year>6)
                     cout<<"Error!,number of years is not in range");
                     cout<<"Enter percentage of down payment :";
                     cin>>b;
                     cout<<"------------------------------------");
    }
    system("pause");
    return 0;
}
