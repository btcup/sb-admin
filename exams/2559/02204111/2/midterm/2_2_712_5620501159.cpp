#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    int x,a,b,c,d;
   cout<<"Enter and integer: ";
   cin>>x;
   
   
  { 
   if (x==x);
   {cout<<"Number of degit is 1"<<endl;}
   if (x==(x*a));
   {cout<<"Number of degit is 2"<<endl;}
   if (x==(x*a*b));
   {cout<<"Number of degit is 3"<<endl;}
   if (x==(x*a*b*c));
   {cout<<"Number of degit is "<<endl;}  
   if (x==(x*a*b*c*d));
   {cout<<"Number of degit is 5"<<endl;}   
   } 
   cout<<" "<<x<<" is a target number."<<endl;
          
   
   

system("pause");
return 0;
}
