//5920503955 Keeratekar Damjapho
#include <iostream>
using namespace std;
int main ()
{
	float per,charge,total;
	int last,current,used;
	last=0,current=0,per=1;
	cout<<"Enter electricity cost per unit(baht):";
	cin>>per;
	do
	If(per>=1)
	{
		cout<<"Enter last unit:";
		cin>>last;
				
					If(last<99999)
		    {
			{cout<<"Enter current unit:";
				 cin>>current;
			}
					If(current<99999){
			
			{
				cout<<"Unit used=";
				cin>>used;
				cout<<"Electricity charge (baht)=";
			}
			total=charge+charge;
			cout<<"Total of electricity charge (baht)="<<total<<endl;
	}
}

	else if(per=-1)
	{cout<<"--End of Calculation--"<<endl;}
	else
	continue;
				}
				else
				{cout<<"Invalid current unit!";}
					}
					else
					{cout<<"Invalid current unit!";}
while(last!=-1)
	system("pause");
	return 0;
}
