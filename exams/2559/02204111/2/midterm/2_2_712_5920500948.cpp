#include <iostream>
#include <cmath>
using namespace std;
int main ()
{
   int n,  i=0, sum1=0, sum2=0, x=1,    y,  sum3;
   cout<<"Enter an integer : ";
   cin>>n;
   do
   {      n=n/10;
       if (n==0);
       i++;
       sum1=i;
       sum2=i+1;
       
   }
   while (n/10!=0);
   cout<<"Number of target is : "<<sum2<<endl;
   y=(n/(pow(10,sum1)));
   sum3=
   
                if(sum3<n )
                cout<<n<<"is a target number"<<endl;
                else
                cout<<n<<"is not target number"<<endl;
   
   cout<<x;
                                
    system ("pause");
    return 0;
    
}
