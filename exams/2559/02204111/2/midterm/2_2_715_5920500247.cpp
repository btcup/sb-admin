#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    int x,y=0,z=0,a,i=100000;
    cout<<"Enter an integer : ";
    cin>>a;
    while(a<=0)
    {
               x=a%10;
               y++
               a=(a-x)10;
               z=a
               z=z+pow(z,y)
    
       system("pause");
       return 0;
}
