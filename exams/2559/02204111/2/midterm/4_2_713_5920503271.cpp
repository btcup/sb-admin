#include <iostream>
using namespace std;
int main()
{
    int z,x,c;
    cout<<"Enter a positive integer: ";
    cin>>z;
    if (z<=0)
    {
     cout<<"Invalid number"<<endl;       
    }
    else if (z>=0)
    {
         cout<<"Number of digit is"<<
         cout<<"Factor of "<<z<<" are :<<endl;
         for (x=1;x<z;x++)
         {
         z%x=0
         }   
    }
    system ("pause");
    return 0;
}
