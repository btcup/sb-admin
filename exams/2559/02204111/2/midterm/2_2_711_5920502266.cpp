//5920502266 Jirada Ngamchantuk
#include <iostream>
#include <cmath>
using namespace std;
int main()
{
	int i,n,a,b,c,d;
	cout << "Enter an integer : ";
	cin >>i;
	cout <<"Number of digit is ";
	cin >>n;
	{
		for (i=i;i>=n&&i<n;i++)
		cout <<"Number of digit is";
		
	}
	
	if(pow(a,a)+pow(b,b)+pow(c,c)+pow(d,d) ==i)
	cout <<i<<"is a target number."<<endl;
	
	if (pow(a,a)+pow(b,b)+pow(c,c)+pow(d,d) !=i)
	cout <<i<<"is not target number."<<endl;
	
	system ("pause");
	return 0;
	
}
