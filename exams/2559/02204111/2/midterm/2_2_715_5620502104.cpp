//5620502104 MissMethinee Kunsatha
#include<iostream>
using namespace std;
int main()
{
    int a=1,b=2,c=3,d=4,e=5,sum;
    
    cout<<"Enter an integer:";
    cin>>a>>b>>c>>d>>e;
  
  
    if(a>0&&b>0&&c>0)
       if(a=3){
       sum=(a*a*a)+(b*b*b)+(c*c*c);
       cout<<"Number of digit is "<<sum<<endl;}
       else{
       cout<<a<<b<<c<<"is not target number.";}
       
       //if(a&&b&&c&&d>0)
      // y=4;
      // cout<<"Number of digit is "<<x<<endl;
      //  if(a&&b&&c&&d&&e>0)
      // z=5;
       //cout<<"Number of digit is "<<x<<endl;
   
   // 
    
    system("pause");
    return 0;
}
