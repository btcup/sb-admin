//"5920503581suchada khampoomuang"
#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    int x,y,z,n,sum=0;
    cout<<"enter an integer:";
    cin>>x;
    n=
    if(sum==x)
    {
         if(n=1)
         cout<<"number of digit is:;
         cout<<sum=pow(x,n)<<sum=pow(y,n)<<sum=pow(z,n);
         cout<<sum<<"is a target number";
         else if(n=2)
         cout<<"number of digit is:";
         cout<<sum=pow(x,n)<<sum=pow(y,n)<<sum=pow(z,n);
         cout<<sum<<"is a target number";
         else if(n=3)
         cout<<"number of digit is:";
         cout<<sum=pow(x,n)<<sum=pow(y,n)<<sum=pow(z,n);
         cout<<sum<<"is a target number";
         else if(n=4)
         cout<<sum<<"number of digit is:";
         cout<<sum=pow(x,n)<<sum=pow(y,n)<<sum=pow(z,n);
         cout<<sum<<"is a target number";
         else if(n=5)
         cout<<"number of digit is:";
         cout<<sum=pow(x,n)<<sum=pow(y,n)<<sum=pow(z,n);
    }
    else
    {
        cuot<<sum<<"is not target number.";
    }
    system("pause");
    return 0;
}
