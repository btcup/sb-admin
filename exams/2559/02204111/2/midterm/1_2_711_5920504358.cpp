#include <iostream>
using namespace std;
int main()
{
    int n,x,y;
    cout<<"Enter N: ";
    cin>>n;
    x=
    if (n=x)
    cout<<"The greatest factor of "<<n<<" is "<<n;
    while 

    
    cout<<"The greatest factor of "<<n<<" is "<<x;
    
    
    
    
    
    
    system ("pause");
    return .0;
}
    
