#include <iostream>

using namespace std;

char mySearch(char x,char y){
     int x;
    cout<<"Enter text :";
    cin<<x;
    cout<<"Enter keyword :";
    cin>>y;
}
    int main()
    char mySearch[x,y]
    cout<<"Enter text :";
    cin<<x;
    cout<<"Enter keyword :";
    cin>>y;
    if(y==x)
    cout<<y<<"is in above text.";
    else
    if(y=!x)
    cout<<y<<"Incorrrect keyword!"<<endl;
    char mySearch.substr(y==x, );

    system("pause")
    return 0;
