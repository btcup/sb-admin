#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    int n ;
    float  a , r ;
    cout<<"Enter n, a and r : "<<endl;
    cin>> n >> a >> r ;
    if (n <= 0 )
    cout<< "n cannot be negative integer and a zero"<<endl;
    else if (a||r = 0)
    cout<< "a and r cannot be zeros"<<endl;
    else if (a = 0)
    cout<< "a cannot be zero"<<endl;
    else if (r = 0)
    cout<< "r cannot be zero"<<endl;
    else if ((n <= 0 )&&(a = 0))
    cout<< "n cannot be negative integer and a zero"<<endl;
    
    system("pause");
    return 0;
}    
   
