#include <iostream>  //5920503769 Tunwa Paksasuk
#include <cmath>
using namespace std;
int main()
{
 	float n,a,r,x;
 	cout<<"Enter n, a and r: ";
 	cin>>n>>a>>r;
 	if(n<=0)
 	cout<<"n connot be both a negative integer and a zero"<<endl;
 	else if((a=0)&&(r=0))
 	cout<<"a and r cannot be zeros"<<endl;
 	else if(a=0)
 	cout<<"a cannot be a zero"<<endl;
 	else if(r=0)
 	cout<<"r cannot be a zero"<<endl;
    else if((n>0)&&(a&&r!=0))
    x=a*(pow(r,i))
    cout<<"The geometric progression of "<<"n= "<<","<<"a= "<<","<<"r= "<<" : "<<x<<endl;
 	system("pause");
 	return 0;
}

