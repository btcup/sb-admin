// 5920500433 Nisarat Lim-udomsap

#include <iostream>
using namespace std;
int main ()
{
 char L,M,S,A;
 float a,b,c;
 if (L);
 cout<<"Enter objective (Living, Agricolture):";
 cin>>L;
 {
  if(L=M)
  cout<<"Enter  living type (Main,Second) :";
  cin>>M;
  if(a>50);
{ cout<<"Enter land cost (million):";
  cin>>a;
  cout<<"Enter building cost (million):"<<((a*0.06)/100)<<endl;
  if (b>=17);
  cout<<"Building age:";
  cin>>b;
  
}
}
else if(L=S)
  cout<<"Enter  living type (Main,Second) :";
  cin>>S;
  if(a>10);
{ cout<<"Enter land cost (million):";
  cin>>a;
  cout<<"Enter building cost (million):"<<a/0.12<<endl;
  if (b>=41);
  cout<<"Building age:";
  cin>>b;
 
}
 system ("pause");
 return 0;

}
