#include<iostream>
using namespace std;
int main()
{
  int n ;
  while (n>0)
  cout<<"Enter n,a and r "<< endl;
  cin>>n>>a>>r;
   if(n==0)&&(a,r!=0);
   cout<<"n cannot be both a negative integer and zero" endl;
   cin>>n!=0;
   cout<<"a cannot be a zero" endl;
   cin>>a!=0;
   cout<<"r cannot be a zero" endl;
   cin>>r!=0;
  cout<<"The geometric progression of n,a,r"  endl;
   cout<<"The summation :"<<endl;  
   cin>>n;
    system ("pause")
    return 0;
}
