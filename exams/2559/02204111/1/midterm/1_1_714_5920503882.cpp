#include <iostream>
#include <cmath>
using  namespace std;
int main()
{
	float x, i, a, d ;
	int n, area ;
	cout<<"Enter n ,  a  and d:";
	cin>>n>>a>>d;
	area=a+(i*d)
	if(n!=0&&a!=0&&d!=0)
	cout<<"The arithmetic progression of";
      if(n=0||a=0)
	cout<<"n cannot be both a negative integer and zero"<<endl;
      alse if (a==0)
	cout<<"a cannot be zero"<<endl;
	alse if (d==0)
	cout<<"d cannot be zero"<<endl;
	
	alse
	cout<<"the summation :"<<area<<endl;
	
	system ("pause");
	return 0 ;
	
	
}
