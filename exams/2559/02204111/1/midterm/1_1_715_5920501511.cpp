//5920501511 Maswimol
#include<iostream>
using namespace std;
int main()
{
    float n,a,d,sum,i=0,s;
    cout<<"Enter n, a and d:"; cin>>n>>a>>d;//continue; 
    if(n!=0)
    {cout<<"The arithmeic progression of "<<"n="<<n<<", "<<"a="<<a<<", "<<"d="<<d<<":";
    {for(i<=n;sum=a+i*d;i=i+1)
    {if(i<=n)
    cout<<sum<<" ";}
    }
    
    } 
    //cout<<"The summation:"<<ts;     

    else
    cout<<"n cannot be both a negative integer and a zero";   

        
    system("pause");
    return 0;
    
}

