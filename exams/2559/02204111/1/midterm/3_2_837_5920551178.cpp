#include<iostream>//5920551178 Hachaturat
using namespace std;
int main()
{
    float  x1,x2,a,b,c;
    cout<<"Enter coefficients a,b and c:";
          cin>>a>>b>>c;
    if(pow(b,2)-4*(a*c)>0)
            x1=(-b+sqrt(pow(b,2)-4*(a*c)));
    
    
    
    
     system ("pause");
    return 0 ;
    
    }    
