//5920502967 Onjira Aiamthip
#include <iostream>
#include <cmath>
using namespace std;
int main ()
{
    flaot x,a,b,c;
    const a,b,c
    pow a*pow(x,2)+(b*x)+c==0
    pow(b,2)-(4*a*c)
    if (pow(b,2)-(4*a*c)==1)
    {x1==[-b+sqrt*pow(b,2)-(4*a*c)]/2a || x2==[-b-sqrt*pow(b,2)-(4*a*c)]/2a};
    cout<<"Eneter coefficients :"<< a << endl;
    cin>> a 
    else if (x1==x2==0)
    {[-b+sqrt*pow(b,2)-(4*a*c)]/2a};
    cout<<"Enter coefficients :"<< b << endl;
    cin>> b
    else(pow(b,2)-(4*a*c)!=1) 
    {x1==-b/2a+(sqrt-(pow(b,2)-4*a*c/2a)i || x2==-b/2a-(sqrt-(pow(b,2-a*a*c)/2*a)i}
    cout<<"Enetr coefficients :"<< c << endl; 
    cin>> c
    system ("pause");
    return 0;
}
    
    
