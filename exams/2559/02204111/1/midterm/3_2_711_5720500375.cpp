#include <iostream>
#include <cmath>
using namespace std;
int main ()
{
    float a,b,c,x,x1,x2;
    cout<<"Enter coefficients a,b and c:";
    cin>>a>>b>>c;
   
   if( a=a*x*x+b*x+c)
   x1=(-b+sqrt(pow(b,2)-4*a*c))/2*a;
   
   cout<<"Roots are real and different."<<endl;
    b=(pow(b,2)-4*a*c);
    
    
    system ("pause");
    return 0;
}
    
