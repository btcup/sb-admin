#include<iostream>
using namespace std;
int main()
{
    int n;
    float a,r;
    cout<<"Enter n, a nd r :";
    cin>>n>>a>>r;
    if(n>0,r>0,r<0,a>0,r>0);cout<<"The geometric progression of n="<<n<<",a="<<a<<",r="<<r<<;
    if(n==0,n<0);cout<<"n cannot be both a negative integer and a zero"<<;
    if(a==0,r==0)<<"a and r cannot be zero";

    
   
                  
               
        


    system("pause");
    return 0;
}    
    
    
