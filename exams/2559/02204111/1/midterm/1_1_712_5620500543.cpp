#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    int n,a,d,sum,i;
    
    cout << "Enter n, a and d: ";
    cin >> n >> a >> d;
    
    while(n<=0 || a<=0 || d<=0)
    {
           cout << "n cannot be both a negative integer and a zero" << endl;;
           cout << "Enter n, a and d: ";
           cin >> n >> a >> d;
           if(n<=0 || a<=0 || d<=0)
           {
             if(n<=0)
             {
                   if(n<=0)
                   {
                    cout << "n cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(a<=0)
                   {
                    cout << "a cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(d<=0)
                   {
                    cout << "d cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(n<=0 && a<=0)
                   {
                    cout << "n cannot be both a negative integer and a zero" << endl;
                    cout << "a cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(n<=0 && a<=0)
                   {
                    cout << "n cannot be both a negative integer and a zero" << endl;
                    cout << "a cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(n<=0 && d<=0)
                   {
                    cout << "n cannot be both a negative integer and a zero" << endl;
                    cout << "d cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(a<=0 && d<=0)
                   {
                    cout << "a cannot be both a negative integer and a zero" << endl;
                    cout << "d cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(n<=0 && a<=0 && d <=0)
                   {
                    cout << "a cannot be both a negative integer and a zero" << endl;
                    cout << "a and d cannot be zeros" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
             }
         }
         else if(a<=0)
             {
                   if(n<=0)
                   {
                    cout << "n cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(a<=0)
                   {
                    cout << "a cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(d<=0)
                   {
                    cout << "d cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(n<=0 && a<=0)
                   {
                    cout << "n cannot be both a negative integer and a zero" << endl;
                    cout << "a cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(n<=0 && a<=0)
                   {
                    cout << "n cannot be both a negative integer and a zero" << endl;
                    cout << "a cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(n<=0 && d<=0)
                   {
                    cout << "n cannot be both a negative integer and a zero" << endl;
                    cout << "d cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(a<=0 && d<=0)
                   {
                    cout << "a cannot be both a negative integer and a zero" << endl;
                    cout << "d cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(n<=0 && a<=0 && d <=0)
                   {
                    cout << "a cannot be both a negative integer and a zero" << endl;
                    cout << "a and d cannot be zeros" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
         }
         else if(d<=0)
             {
                   if(n<=0)
                   {
                    cout << "n cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(a<=0)
                   {
                    cout << "a cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(d<=0)
                   {
                    cout << "d cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(n<=0 && a<=0)
                   {
                    cout << "n cannot be both a negative integer and a zero" << endl;
                    cout << "a cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(n<=0 && a<=0)
                   {
                    cout << "n cannot be both a negative integer and a zero" << endl;
                    cout << "a cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(n<=0 && d<=0)
                   {
                    cout << "n cannot be both a negative integer and a zero" << endl;
                    cout << "d cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(a<=0 && d<=0)
                   {
                    cout << "a cannot be both a negative integer and a zero" << endl;
                    cout << "d cannot be both a negative integer and a zero" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
                   else if(n<=0 && a<=0 && d <=0)
                   {
                    cout << "a cannot be both a negative integer and a zero" << endl;
                    cout << "a and d cannot be zeros" << endl;
                    cout << "Enter n, a and d: ";
                    cin >> n >> a >> d; 
                   }
             }
         }
    cout << "The arithmrtric progression of " << " n = " << n << " , a = " << a << " , d= " << d << endl;;
                 for(i=1; i<=n ; i++ )
                 {
                          sum = a+(i*d);
                          cout << " ";
                 }
                  cout << sum << endl;
     
     system("pause");
     return 0;
 }   
