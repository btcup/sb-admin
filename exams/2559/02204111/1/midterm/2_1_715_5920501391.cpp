#include <iostream>
using namespace std;
int main()
{
    int n,a,r,x,y;
    cout<<"Enter n,a and r:";
    cin>>x;
     if(n>=1)
    cout<<"The geometric progassion of n= :\n";
    cin>>n;
    if(a>=1)
    cout<<"The geometric progassion of a= :\n";
    cin>>a;
    if(r>=1)
    cout<<"The geometric progassion of r= :\n";
    cin>>r;
    if(n==0||n<0)
    cout<<"n cannot be both a negative integer and a zero";
    else
    
    system("pause");
    return 0;
}
