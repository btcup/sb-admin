#include<iostream>
using namespace std;
int main(){
    
    double n,a,d,i,X;
    i = n-1;
    cout<<"Enter n,a and d";
    cin>>n>>a>>d;
    
    if(X=a+(i*d)&&n>0)
    cout<<"X ="<<X<<endl;
    else if(n<=0)
    cout<<"n cannot be both a negative integer and a zero"<<endl;

system ("pause");
return 0;
}
    
