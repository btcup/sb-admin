#include <iostraem>
using namespace std;
int main()
{
    double n,a,d;
    cout<<"Enter n,a and d :";
    cin>>n>>a>>d;
    if(n>0 && a!=0 && d!=0)
    
    cout<<"The arithmet progression of  n="<<n<<", a="<<a<<", d="<<d<<":"<<endl;
    cout<<
  
    
     else if (n<=0)
     cout<<"n can not be both a negative integer and a zero";
     else if (n<=0 && a==0 && d==0 )
     cout<<"n can not be both a negative integer and a zero\t a and d csnnot be zeros";
     else if (a==0)
     cout<<"a cannot be a zero";
     else if (d==0)
     cout<<"d cannot be a zero";
     
     
      
    


    cout<<"Enter n, a and d :";
    cin>>n>>a>>d;
    system("pause");
    return 0;
}
    
