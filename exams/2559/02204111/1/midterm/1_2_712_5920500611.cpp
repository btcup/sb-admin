#include <iostream>
using namespace std;
int main()
{ 
char a,b;
int m,s,p,x,o;
cout<<"Enter objective (Living, Agriculture) :";
cin>>a;
    cout<<"Enter living type (Main, Second) :";
    cin>>b;
           cout<<"Enter land cost (million) :";
           cin>>m;
           cout<<"Enter building cost (million) :";
           cin>>x;
           cout<<"Building age :";
           cin>>o;
           if(b=M){
                   if(m<50);cout<<"0";
                   else if(m>=50&&m<100);cout<<s=m/0.06;
                   if(m>=100);cout<<s=m/0.12;
                   }
           if(b=S){
                   if(m<50;cout<<s=m/0.3;
                   else if(m>=5&&m<10);cout<<s=m/0.6;
                   if(m>10);cout<<s=m/0.9;
                   }
                   }
           if (b==A){
                     if(b<40);cout<<"0";
                     eise if(b>=40&&b<80);cout<<s=m/0.04;
                     else if(b>80);cout<<s=m/0.8
    system ("pause");
return 0;
}
                   
    
           
